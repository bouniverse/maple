# 세션 핸드오프 — 집/다른 기기에서 이어받기용

> 갱신: 2026-06-04 (회사 Windows) · Repo: https://github.com/bouniverse/maple
> 새 세션에서 이 파일 먼저 읽으면 현재 상태·다음 액션 바로 파악됨.
> 함께 볼 것: `_progress/스탯-측정.md`(최신 측정), `_progress/기획안.md`, `_progress/데이터.md`

---

## 0. 한 줄 상태
**스탯 측정 + 엔진 통합 완료(2026-06-04).** 주요 변수 모델 확정·코드 반영·테스트 13/13 통과. **다음 = 추가 측정(보스뎀%/방어관통/크리저항) 또는 계산기 UI 설계.**

---

## 1. 2026-06-04 측정 성과 (스탯-측정.md에 상세)

인게임 단일 변수 측정으로 확정 (통제: 동료 OFF · 스킬 OFF=기본공격 · 럭키다이스 변동분 0 최소값):

| 변수 | 결과 | 특성 |
|------|------|------|
| 공격력 +1 → 실데미지 | +0.0000149% (= 1/공격력) | 선형 |
| 주스탯(DEX) +1 → 실데미지 | **+0.0062%** (공격력경로 23% + 스탯비례경로 77%) | 거의 선형 |
| 데미지 +1%p → 실데미지 | **+0.307%** (현재 225.8% 기준) | **비선형** |
| 크리 배율 | **2.0×** (크리뎀 100%, 실측 2.006× 검증) | — |
| 스탯비례데미지(%) | **= 주스탯 ÷ 100** (계수 0.01004) | 공식 발견 |
| 무기 레벨 | 장착효과 **+8.6%p/레벨** | 선형 |
| 럭키다이스 | 고정 +25.5% + 변동 평균 +2.6% | 변동분 캐릭터창 반영 |

**핵심 방법론 교훈:** 실제 타격 데미지값은 데미지범위(131~154.6%)+럭키다이스로 ±25% 출렁여서 **수%짜리 옵션 검증 불가**. → **캐릭터창 스탯값(정확) + 공식**으로 계산하는 것이 정답.

---

## 2. ✅ 엔진 통합 완료 (구버전 §2 미결정 해소)

`engine/realDamage.mjs` 통합 리팩터링 완료:
- **공격력 입력 이원화**: `atkFinal` 제공 → 스냅샷 모드(직접 사용, perLevel OFF) / 미제공 → 델타 모드(totalAttack 파생, perLevel ON). 이중계산 방지.
- **헬퍼 추가**: `statRatioFromMainStat(mainStat)` (×0.01004), `atkFromDex(dexDelta)` (×92.3 [가설]).
- **럭키다이스 옵션**: `options.luckyDiceAvg=true` → ×1.026 (기댓값).
- **측정 근거 주석** 코드에 명기.
- `validate-necklace.mjs` → 본 엔진 import로 교체, 기존 결과(A>B>C, 0/-1.76%/-2.02%) **완전 재현**.
- 테스트 **13/13 통과** (`node engine/realDamage.test.mjs`). 직접 실행 검증 완료.
- 상세: `tmp/engine_integration_output.md`

> ⚠ 미커밋 상태 — 대표님 "커밋" 지시 시 add/commit/push.

---

## 3. 다음 액션 (다음 세션 첫 후보)

- **A. 추가 측정** — 보스 몬스터 데미지% · 방어 관통 · 크리티컬 저항 등 딥 변수. (측정 프로토콜 확립됨 → 동일 방식)
- **B. 계산기 UI 설계** — 측정·엔진이 섰으니 "바꿀 것만 입력 → 실데미지 변화율" 화면 설계. (게임 화면 가로/세로 미확인 = UI 블로커, 데이터.md §5)
- **C. perLevel 실측** — 델타 모드 정확도용. 3차 스킬 레벨만 ±1~2 변경 전/후 최종 공격력.

---

## 4. 미확정/미보정 (스탯-측정.md §4 + engine 주석)
1. perLevel(스킬레벨당 %) — [미보정], calibratePerLevel로 역산 가능
2. atkFromDex 계수 92.3 — [가설] (어빌 명목+300 vs 실제 DEX+327, 주스탯 %증폭 추정)
3. luckyDiceAvg +2.6% — [가설] 시간가중 평균, 변동분 min/max 정밀 측정 시 확정
4. 방어 관통 — 적 방어력 입력 필요, 엔진 미반영
5. 게임 화면 가로/세로 미확인 → UI 설계 블로커

---

## 5. 환경 메모
- 집 = Mac(개인, Claude Pro). 회사 = Windows(Claude Enterprise).
- 실행: `node engine/realDamage.test.mjs`, `node engine/validate-necklace.mjs`
- git: `이비오 / bio3698@gmail.com`, 기본 브랜치 `main`.
- 측정 스크린샷은 `screenshot/`에 임시 업로드 → 판독 후 새 측정 시 덮어씀.
