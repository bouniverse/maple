# MAPLE — 메이플키우기 실데미지 계산기

> 넥슨 [메이플키우기] 방치형 IP 기반. **전투력(뻥투력)이 아닌 실데미지·사냥속도로 장비/업글 가치를 판단**하는 계산기.
> 메키픽/메키펀 상위호환 + 포트폴리오 + 도메인운영/광고. 역할: 비오=기획·디자인·프론트, Claude=계산엔진·공식검증.

---

## 목표
유저가 자기 스탯을 입력 → "이 장비·업글이 **실제로 내 딜/사냥속도를 올리는가**"를 전투력 숫자에 속지 않고 비교.
- 현재 스탯 1회 입력 + "바꿀 것만(델타)" → 결과. (목표수치 칸 없음)
- 실데미지 vs 전투력 **분리 표시**, 딜 스탯 / 뻥·생존 스탯 **구분**.

## 핵심 제약
- 공식 오픈 API 없음 → 모든 데이터는 **본인 정보창 수동 입력**이 유일·최선. 랭킹 API는 전투력/직업/레벨만.
- 검증은 항상 **실데미지**로. 전투력은 HP·방어 등 생존스탯까지 포함해 부풀려짐 → 신뢰 금지.
- 측정 시 **전체 스탯 스냅샷 + 다이스 상태** 함께 기록(절대값 정확도용 규율).

---

## 확정된 자산 (검증 완료)
- **데미지 공식**: 전 항목 곱셈. `engine/realDamage.mjs` 16/16 테스트. 상대비교 정확.
- **크리 배율** = 1 + 크확×크뎀. 단일 크리타격 = ×(1+크뎀/100).
- **일반몹 방어곡선(16존)**: 선형 `0.1915g−0.385`(R²0.995) / 2차 `−2.591e-4g²+0.2358g−1.691`(R²0.998, 후반 둔화). g=(존−1)×10+스텝. 방어=최종데미지 곱셈 감쇄. **성장후 재검증 완료**(30.17%=측정30.16%).
- **34% 절대스케일 갭** = stat-independent 상수 → 상대비교 무관(약분), 절대값엔 ×1.34 보정.
- **곱셈 한계기여**: 현재 낮은 항(증폭·최종데미지)이 최고효율, 높은 항(데미지%)이 최저. → `_progress/실데미지-옵션-우선순위.md`.
- **딜량 ≠ 사냥속도**: 원킬 구간선 추가 데미지가 사냥속도 0 기여 → 2국면 모델(`clearTime/huntRate/overkillThresholdG`).

## 진행 단계 (대표님 우선순위)
- **Phase 1 사냥딜** [진행중]: 방어곡선·한계기여 ✅ / 측정 대기 = ①몬스터HP(원킬임계) ②공속→초당타격수 ③스킬 AoE 마릿수.
- **Phase 2 보스딜**: 보스 방어곡선 적합 + 방어관통 측정.
- **Phase 3 PVP**: 받는피해감소 등.
- **Phase 4 제품화**: MVP HTML 반입 + 엔진동기화, 가로게임→세로9:16 UI(ux-team 소환).

---

## 모델 사용 규칙
- **실행(측정 입력·스크립트·회귀·구현)은 sonnet 서브에이전트에 위임.** opus는 executor 금지.
  - `measurement-analyst`(sonnet): 측정값 입력→회귀/환산/검증/기록 전담. (`.claude/agents/`)
- **opus는 advisor(조언)만** — 실험설계·산출물 모순중재·방향 갈림길·갭 진단. 세션당 최대 3회, 질문은 한 문장.
- 단순 읽기/검색은 haiku.
- 서브에이전트 산출물은 `tmp/`에 저장, 메인엔 한 줄 보고(긴 코드 붙여넣기 금지).

## 검증 규율 (수학 의존 리스크 완화 — 핵심)
공식은 Claude에 크게 의존하므로 정확성은 에이전트가 아니라 **검증 레이어**로 지킨다:
1. 모든 공식 결론은 **게임 실측으로 채점**(예: 성장후 2-1·16-5 비율로 방어율 재현).
2. 중요 분기·갭은 **advisor(opus) 교차검증**.
3. 엔진은 **테스트로 회귀 방지**(`node engine/realDamage.test.mjs`).

## 작업 규칙
- 측정/분석 작업은 yes/no 승인 없이 바로 실행 후 보고.
- 공격력 수치 받으면 럭키다이스 최소값(변동분0)인지 확인.
- 즉시 구현 금지 — 이해 한 줄 + 질문 최대 2개 → 실행. ("바로 해줘" 시 질문 생략)

## 파일 맵 (어디를 보면 되나)
```
engine/realDamage.mjs       데미지·방어·사냥속도 엔진 (+ baseline.mjs, *.test.mjs)
_progress/
  SESSION-HANDOFF.md        세션 진입점 — 현재 상태·다음 액션
  스탯-측정.md               모든 측정 결과 누적 (방어곡선·크뎀·갭)
  실데미지-옵션-우선순위.md    옵션별 실뎀 한계기여·어빌 순위
  회고-로드맵.md             회고 + Phase 로드맵
  기획안.md / 데이터.md       제품 결정 / 공식·스탯 분류
tmp/                        서브에이전트 임시 산출물
```

## AI 사용 기재 (포트폴리오 제출용)
- 기획·구조·계산엔진·공식검증 보조: Claude / 시각디자인(Figma): 본인 직접 / 이미지생성 AI: 미사용.
